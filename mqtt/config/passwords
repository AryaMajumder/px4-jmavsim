sudo apt install -y mosquitto-clients
docker run --rm -it eclipse-mosquitto mosquitto_passwd -c /tmp/passfile drone
# enter a password (e.g. dronepass)
docker run --rm -it eclipse-mosquitto mosquitto_passwd -b /tmp/passfile consumer consumerpass
